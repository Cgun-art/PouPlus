import { createRequire } from 'module';

const require = createRequire(import.meta.url);
const { exec } = require('child_process');

console.log('Shiddin, Aho!, pwamp.');

libraries(shareWithOtgherProgrammingLanguages);
